This folder contains generated Kaggle submission files.

Starter file:

- `working_submission.csv`: editable submission template with columns `id,return_pct`

Regenerate it with:

```bash
python scripts/create_submission_template.py
```
